<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:TsinghuaNet" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting" xmlns:Series="using:WinRTXamlToolkit.Controls.DataVisualization.Charting" xmlns:datavis="using:WinRTXamlToolkit.Controls.DataVisualization" x:Class="TsinghuaNet.MainPage" mc:Ignorable="d" Language="zh-cn" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
            <TextBox x:Name="textboxUserName" PlaceholderText="用户名" Height="32" Width="200" Margin="10" InputScope="AlphanumericHalfWidth" />
            <PasswordBox x:Name="passwordboxPassword" PlaceholderText="密码" Height="32" Width="200" Margin="10" IsPasswordRevealButtonEnabled="True" PasswordChanged="passwordboxPassword_PasswordChanged" GotFocus="passwordboxPassword_GotFocus" />
            <Button Click="Button_Click" HorizontalAlignment="Center" Width="200" Height="40" Margin="10" Content="登陆" />
            <Button Click="test" HorizontalAlignment="Right" Width="200" Height="40" Margin="10" Content="test" />
        </StackPanel>
        <ListView x:Name="listviewDevices" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10" Orientation="Horizontal">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" />
                            <TextBlock Text="{Binding IPAddress}" />
                            <TextBlock Text="{Binding WebTraffic}" />
                        </StackPanel>
                        <Button x:Name="buttonLogOff" Tag="{Binding}" Content="下线">
                            <Button.Flyout>
                                <Flyout>
                                    <StackPanel>
                                        <TextBlock>
											<Run Text="确定？" />
                                        </TextBlock>
                                        <Button Click="MenuFlyoutItem_Click" Tag="{Binding ElementName=buttonLogOff}" Content="是" />
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                        <Button x:Name="buttonRename" Tag="{Binding}" Content="重命名">
                            <Button.Flyout>
                                <Flyout>
                                    <StackPanel>
                                        <TextBox x:Name="textboxRename" PlaceholderText="新名称" />
                                        <Button Click="Button_Click_1" Tag="{Binding ElementName=buttonRename}" Content="确定" />
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Click="Button_Click_2" Margin="50" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Content="刷新" />
        <charting:Chart x:Name="MixedChart" Title="Mixed Chart" Margin="300,0,20,0">
            <charting:ColumnSeries x:Name="aa" Title="aaa" IndependentValueBinding="{Binding Key}" DependentValueBinding="{Binding Value.Value}" >
                <Series:ColumnSeries.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition />
                    </TransitionCollection>
                </Series:ColumnSeries.Transitions>
            </charting:ColumnSeries>
        </charting:Chart>
    </Grid>
</Page>
