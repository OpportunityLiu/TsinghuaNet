<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:TsinghuaNet" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting" xmlns:Series="using:WinRTXamlToolkit.Controls.DataVisualization.Charting" xmlns:datavis="using:WinRTXamlToolkit.Controls.DataVisualization" x:Name="page" x:Class="TsinghuaNet.MainPage" mc:Ignorable="d" Language="zh-cn" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="page_SizeChanged">
    <Page.Resources>
        <Style x:Key="HubSectionStyle" TargetType="HubSection">
            <Setter Property="Transitions">
                <Setter.Value>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="100" />
                    </TransitionCollection>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="OnlineDeviceItemTemplate">
        	<StackPanel Margin="5">
        		<TextBlock Text="{Binding Name}" Margin="5"/>
        		<TextBlock Text="{Binding WebTraffic}" Margin="5"/>
        	</StackPanel>
        </DataTemplate>
    </Page.Resources>
	<Page.BottomAppBar>
		<CommandBar>
			<CommandBar.SecondaryCommands>
				<AppBarButton Icon="Sync" Label="更换用户"/>
			</CommandBar.SecondaryCommands>
			<AppBarButton Icon="Rename" Label="重命名"/>
			<AppBarButton Icon="Stop" Label="下线"/>
		</CommandBar>
	</Page.BottomAppBar>
    <Hub x:Name="hub" Header="Tsinghua Net">
        <HubSection x:Name="hubSectionPic" Header="" HorizontalAlignment="Left" Width="500" Margin="-200,0,0,0">
            <HubSection.Background>
                <ImageBrush Stretch="UniformToFill" ImageSource="Images/Header.png" />
            </HubSection.Background>
            <DataTemplate />
        </HubSection>
        <HubSection x:Name="hubSectionStart" Header="开始使用" Style="{StaticResource HubSectionStyle}">
            <DataTemplate>
                <StackPanel Width="250">
                    <TextBlock Text="输入清华大学校园网用户名与密码以开始使用。" VerticalAlignment="Top" FontSize="16" Margin="0,10" TextWrapping="WrapWholeWords" />
                    <TextBox Margin="0,10" PlaceholderText="用户名" />
                    <PasswordBox Margin="0,10" PlaceholderText="密码" />
                    <Button Content="确定" Margin="0,10" HorizontalAlignment="Stretch" Click="Button_Click" />
                </StackPanel>
            </DataTemplate>
        </HubSection>
        <HubSection x:Name="hubSectionState" Header="{Binding Username}" Style="{StaticResource HubSectionStyle}">
            <DataTemplate>
                <StackPanel Margin="0,5" Width="350">
                    <TextBlock FontSize="16" Margin="0,5">
						<Run Text="流量    " />
						<Bold>
							<Run Text="[流量]" />
						</Bold>
                    </TextBlock>
                    <ProgressBar Height="10" VerticalAlignment="Stretch" BorderThickness="0" Margin="0,5,0,15" Maximum="50"/>
                    <TextBlock FontSize="16" Margin="0,5">
						<Run Text="余额    " />
						<Bold>
							<Run Text="[余额]" />
						</Bold>
                    </TextBlock>
                    <ListView Header="当前设备" FontSize="17.333" Margin="0,30,0,10" ItemTemplate="{StaticResource OnlineDeviceItemTemplate}" ItemsSource="{Binding}" />
                </StackPanel>
            </DataTemplate>
        </HubSection>
        <HubSection x:Name="hubSectionHistory" Header="历史记录" Style="{StaticResource HubSectionStyle}">
        	<DataTemplate>
        		<Grid>
        			<Series:Chart HorizontalAlignment="Left" VerticalAlignment="Top" Width="100" Height="100">
        				<Series:BarSeries/>
        			</Series:Chart>
        		</Grid>
        	</DataTemplate>
        </HubSection>
    </Hub>
</Page>
